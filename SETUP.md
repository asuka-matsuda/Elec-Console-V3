# Elec-Console-v2 環境構築ガイド (別PC移行用)

このドキュメントは、別のPCでこのプロジェクト（Elec-Console-v2）の開発環境を復元し、AI（Antigravity）と一緒に作業を再開するための手順をまとめたものです。

## 1. 事前準備（新しいPCにインストールするもの）

以下のソフトウェアを新しいPCにインストールしてください。

1. **Node.js** (必須)
   - Nuxt3を動かすために必要です。
   - [公式サイト](https://nodejs.org/ja/) から最新の推奨版（LTS版・v20以上推奨）をインストールしてください。
2. **Google Antigravity (AGY)** (必須)
   - AIと一緒に開発を進めるためのツールです。現在のPCと同じようにインストール・ログインしてください。
3. **OneDrive** (必須)
   - プロジェクトのソースコードを同期するために必要です。現在と同じアカウント（Mat.Operate）でログインし、このプロジェクトフォルダ（`elec-console-v2`）が新しいPCに同期されるのを待ちます。

---

## 2. 開発環境のセットアップ

OneDriveの同期が完了し、プロジェクトフォルダが見えるようになったら、以下の手順で環境を構築します。

1. 新しいPCでターミナル（PowerShell や コマンドプロンプト）を開きます。
2. このプロジェクトのフォルダ（`elec-console-v2`）に移動します。
3. 以下のコマンドを実行して、必要なパッケージ（依存関係）をすべてダウンロードします。
   ```bash
   npm install
   ```
   ※この作業は最初の1回だけでOKです。
4. インストールが完了したら、以下のコマンドでローカルサーバーを起動します。
   ```bash
   npm run dev
   ```
5. ブラウザで `http://localhost:3000` にアクセスし、アプリが表示されれば成功です！

---

## 3. AI（Antigravity）との作業再開方法

Antigravityのチャット履歴はPCローカルに保存されているため、新しいPCでは「初めまして」の状態からスタートします。しかし、コード自体にこれまでの文脈や設定がすべて残っているため、以下のステップですぐに以前の環境を再現できます。

1. 新しいPCの Antigravity アプリを立ち上げます。
2. OneDriveで同期された `elec-console-v2` フォルダをワークスペースとして開きます。
3. チャットで次のように話しかけてください。
   > 「OneDriveから移行しました。プロジェクト内のコードを読み込んで、続きの作業をお願いします」
4. AIが最新のコードを読み込み、「PCファーストでの実装」や「コンポーネントの構造」などを理解した上で、そのまま続きの作業を再開できます。

※さらにAIにプロジェクト固有のルールを恒久的に覚えさせたい場合は、今後のチャットで `/learn` コマンドを使用し、「プロジェクトのルールとして保存して」と指示することで、フォルダ内にAI専用のルールファイルを作成させることも可能です（このファイルもOneDriveで同期されます）。
